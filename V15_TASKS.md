# V1.5 后端加固 · 任务地图

> 配套"版本化小步迭代"流程使用。**每完成一项**：启动服务跑一遍确认原有功能没坏 → `git commit` → 勾选这一项。
> 遇到卡点记在文末"阻塞记录"，别硬扛。

## 总验收标准（V1.5 做完 = 能演示这条链路）

```
启动服务 → 登录拿 token → 带 token 聊天 → 对话记录能在 MySQL 查到 → 跑评测脚本能出准确率数字
```

---

## 任务清单

### [ ] 任务 1：修复 calculator 的 eval 安全漏洞
- **做什么**：替换 `tools.py:14` 的 `eval()`，改用安全求值（`ast` 解析 + 只放行数字/四则运算符号，或用安全替代库）。
- **验收标准**：`calculator("500*3")` 正常返回 1500；恶意输入如 `__import__('os').system('...')` 被拒绝且服务不崩。
- **预计耗时**：15 分钟
- **面试能讲**：eval 为什么危险、ast 白名单方案的思路（安全意识是加分项）。

### [ ] 任务 2：lifespan 启动时建一次 graph
- **做什么**：`main.py` 用 FastAPI `lifespan` 在启动时 compile 一次图并存 `app.state.graph`，`/chat/stream` 和 `/chat/once` 改为复用。
- **验收标准**：连续请求多次，日志里只编译一次；原有聊天、流式功能无回归。
- **预计耗时**：30 分钟
- **面试能讲**：资源复用、为什么"每个请求重编译"是低效写法、lifespan 的正确姿势。

### [ ] 任务 3：统一响应格式 + 全局异常处理
- **做什么**：定义统一结构 `{code, message, data}`；加全局 `ExceptionHandler`，把 LLM/工具异常转成友好 JSON。
- **验收标准**：正常请求返回统一格式；故意传非法参数触发错误时，返回统一错误 JSON，而不是裸 500。
- **预计耗时**：1 小时
- **面试能讲**：API 规范设计、错误处理策略（这是"后端能力"的直观证据）。

### [ ] 任务 4：补全 .env.example + 阈值下沉为配置
- **做什么**：整理 `.env.example` 模板；把 `search_kb` 里硬编码的 `0.9` 阈值改成读配置。
- **验收标准**：新环境只按 `.env.example` 一份配置就能跑通；阈值改配置后生效。
- **预计耗时**：15-30 分钟
- **面试能讲**：配置管理意识、不写死魔数。

### [ ] 任务 5：对话记录落 MySQL
- **做什么**：SQLAlchemy 2.x 异步接入；建 `chat_history` 表；每轮对话写入；提供查询接口（存 SQLite 的迁移/并存）。
- **验收标准**：聊完几轮后能在 MySQL 里查到记录；服务重启后记录仍在。
- **预计耗时**：2-3 小时
- **面试能讲**：SQLite→MySQL 迁移过程、连接池、ORM 与原生 SQL 取舍。

### [ ] 任务 6：JWT 鉴权保护接口
- **做什么**：`/auth/register`、`/auth/login` 签发 token；密码用 bcrypt 哈希（不存明文）；`Depends(get_current_user)` 保护聊天接口。
- **验收标准**：无 token 访问聊天接口返回 401；带合法 token 正常；过期 token 被拒。
- **预计耗时**：2 小时
- **面试能讲**：JWT 三段式原理、与 Session 的区别、token 存放位置（这是后端必考题）。

### [ ] 任务 7：补关键单测
- **做什么**：pytest 覆盖 `calculator`（正常/恶意输入）、`search_kb` 过滤逻辑、两个聊天接口的返回结构。
- **验收标准**：`pytest` 全绿；有测试文件在项目里（你现在一个都没有——用你的测试背景把这个短板补上）。
- **预计耗时**：2 小时
- **面试能讲**：测试思维做 AI 应用、边界用例设计（你的差异化标签，必须体现在代码里）。

### [ ] 任务 8：RAG 评测脚本
- **做什么**：从 `data/knowledge.txt` 手工构造 30-50 组「问题 → 标准答案/相关片段」；写评测脚本算召回命中率 + LLM-as-judge 打分；结果存 `eval/` 目录。
- **验收标准**：脚本可反复运行；输出评测报告；README 贴评测结果表格——给你简历的"准确率 85%/95%"上出处。
- **预计耗时**：3-4 小时
- **面试能讲**：评测方法论、数字从哪来、怎么验证不幻觉（这条比数字本身更有说服力）。

### [ ] 任务 9：阶段验收 + README 同步
- **做什么**：按顶部"总验收标准"完整演示一遍；README 更新（新接口、新架构、为在线 demo 截图留位）；git 提交。
- **验收标准**：总验收标准逐条通过；README 反映 V1.5 真实现状。
- **预计耗时**：1 小时

---

## 节奏建议

- **任务 1-4**：都是小改动，一天内能全部做完——用来建立"改→跑→验→提交"的节奏。
- **任务 5-6**：后端重头戏，各留半天，别赶。
- **任务 7-8**：你的差异化所在，**别跳过**（一个会测试的应届生，项目里却没有测试，是最可惜的短板）。
- 每完成一项就 `git commit`，提交信息写清楚（如 `fix: 修复 calculator 的 eval 安全漏洞`）。

---

# V1.8 测试管理平台 · 任务地图（进行中）

> 定位：把项目从"纯 Agent"升级为"后端 + Agent"。补齐后端基本功（分层/RESTful/一对多关系/分页），AI 生成用例是 Agent 招牌。

- [x] 结构重构：router/model/schema/service 分层，搬代码不改功能
- [ ] Project + TestCase CRUD：两张表 + RESTful 增删改查 + 分页/过滤
- [ ] AI 生成用例：输入需求 → Agent 生成多条用例 → 入库（Agent 招牌）
- [ ] Execution 执行记录：实例执行落记录，按 case 查执行历史
- [ ] Dashboard 统计：聚合查询（项目数/用例数/通过率/优先级分布）
- [ ] User 扩展 + 系统设置：个人信息、设置

---

## 阻塞记录

（遇到问题写这里：什么任务 / 卡在哪 / 报错信息）
